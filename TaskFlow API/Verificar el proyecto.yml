info:
  name: "Verificar el proyecto "
  type: http
  seq: 1

http:
  method: GET
  url: " http://localhost:3000/api/projects/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
