info:
  name: Probar  User
  type: http
  seq: 1

http:
  method: GET
  url: "URL: http://localhost:3000/api/users "
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
