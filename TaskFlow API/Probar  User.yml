info:
  name: Probar  User
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/users/id-que-no-existe
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
